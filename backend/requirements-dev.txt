# ── Dev / local-only dependencies ──────────────────────────────
# These are NOT needed for the deployed backend (Render free tier).
# Playwright browsers consume ~500 MB and break Render's build.
#
# Install for local scraping:
#   pip install -r requirements-dev.txt
#   playwright install chromium

scrapling[fetchers]>=0.2.99
